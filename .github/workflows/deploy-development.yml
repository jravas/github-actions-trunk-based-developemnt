name: Deploy to development

on:
    push:
      branches:
        - main

jobs:
  call-workflow:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      projectId: kulen-dayz-developemnt
    secrets:
      repoToken: '${{ secrets.GITHUB_TOKEN }}'
      firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KULEN_DAYZ_DEVELOPEMNT }}'